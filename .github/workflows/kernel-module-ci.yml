name: Kernel Module Build and Security Tests

on:
  push:
    paths:
      - 'src/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-aarch64-linux-gnu make bc bison flex libssl-dev libelf-dev cppcheck

    - name: Extract Pi kernel headers
      run: |
        echo "=== Tarball contents ==="
        tar -tzf rpi-headers-6.12.75.tar.gz | head -20
        echo "=== Extracting ==="
        sudo mkdir -p /usr/src
        sudo tar -xzf rpi-headers-6.12.75.tar.gz -C /usr/src
        echo "=== Result ==="
        ls /usr/src/ || true
        ls /usr/src/linux-kbuild-6.12.75+rpt/scripts/ 2>/dev/null || echo "kbuild not here"
        find /usr/src -name "Makefile.extrawarn" 2>/dev/null || echo "extrawarn not found"

    - name: Build kernel module
      run: |
        sudo ln -sf /usr/src/linux-kbuild-6.12.75+rpt/scripts /usr/src/linux-headers-6.12.75+rpt-common-rpi/scripts
        sudo ln -sf /usr/src/linux-kbuild-6.12.75+rpt/tools /usr/src/linux-headers-6.12.75+rpt-common-rpi/tools
        cd /usr/src/linux-headers-6.12.75+rpt-common-rpi
        sudo cp $GITHUB_WORKSPACE/src/kernel.config .config
        sudo make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- olddefconfig
        sudo make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- modules_prepare
        cd $GITHUB_WORKSPACE/src
        make KDIR=/usr/src/linux-headers-6.12.75+rpt-common-rpi ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- KBUILD_MODPOST_WARN=1
        echo "✓ Module built"
        find $GITHUB_WORKSPACE/src -name "*.ko"

    - name: Check vermagic
      run: modinfo $(find $GITHUB_WORKSPACE/src -name "*.ko" | head -1) | grep vermagic

    - name: Run cppcheck
      run: |
        cppcheck --suppress=missingIncludeSystem --error-exitcode=1 --force src/led_on.c
        echo "✅ PASS: cppcheck"

    - name: Check dangerous patterns
      run: |
        python3 << 'PYEOF'
        import sys
        from pathlib import Path
        errors = []
        for cfile in Path("src").glob("*.c"):
            if cfile.name.endswith('.mod.c'): continue
            source = cfile.read_text()
            for fn in ["strcpy", "sprintf", "gets", "strcat", "scanf"]:
                if fn + "(" in source:
                    errors.append(f"❌ {cfile.name}: Dangerous function {fn}()")
            if "MODULE_LICENSE" not in source:
                errors.append(f"❌ {cfile.name}: Missing MODULE_LICENSE")
            if "gpio_request" in source and "gpio_free" not in source:
                errors.append(f"❌ {cfile.name}: gpio_request without gpio_free")
        if errors:
            for e in errors: print(e)
            sys.exit(1)
        print("✅ PASS: No dangerous patterns found")
        PYEOF

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dilithium-py
      run: pip install dilithium-py

    - name: Run hash integrity test
      run: |
        python3 << 'PYEOF'
        import sys, hashlib, tempfile
        from pathlib import Path
        module = next(Path("src").glob("*.ko"))
        def sha3_256_file(path):
            return hashlib.sha3_256(path.read_bytes()).digest()
        original_hash = sha3_256_file(module)
        data = module.read_bytes()
        tampered = bytearray(data)
        tampered[len(tampered)//2] ^= 0xFF
        with tempfile.NamedTemporaryFile(delete=False) as tmp:
            tmp.write(bytes(tampered))
            tmp_path = Path(tmp.name)
        tampered_hash = sha3_256_file(tmp_path)
        tmp_path.unlink()
        if original_hash == tampered_hash:
            sys.exit(1)
        print("✅ PASS: Hash integrity verification works")
        PYEOF

    - name: Run signature tampering test
      run: |
        python3 << 'PYEOF'
        import sys, hashlib
        from pathlib import Path
        from dilithium_py.dilithium import Dilithium2
        module = next(Path("src").glob("*.ko"))
        def sha3_256_file(path):
            return hashlib.sha3_256(path.read_bytes()).digest()
        pk, sk = Dilithium2.keygen()
        artifact_hash = sha3_256_file(module)
        signature = Dilithium2.sign(sk, artifact_hash)
        if not Dilithium2.verify(pk, artifact_hash, signature):
            sys.exit(1)
        tampered_sig = bytearray(signature)
        tampered_sig[100] ^= 0xFF
        if Dilithium2.verify(pk, artifact_hash, bytes(tampered_sig)):
            sys.exit(1)
        print("✅ PASS: Signature tampering detection works")
        PYEOF

    - name: Detect version
      id: detect
      run: |
        LATEST=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
          https://api.github.com/repos/${{ github.repository }}/releases/latest \
          | python3 -c "import sys,json; d=json.load(sys.stdin); print(d.get('tag_name','v0').lstrip('v'))" 2>/dev/null || echo "0")
        VERSION=$((LATEST + 1))
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "📦 New version: $VERSION"

    - name: Prepare release files
      run: |
        VERSION=${{ steps.detect.outputs.version }}
        mkdir -p releases/v${VERSION}
        KO_FILE=$(find $GITHUB_WORKSPACE/src -name "*.ko" | head -1)
        cp $KO_FILE releases/v${VERSION}/module.ko
        echo "${VERSION}" > releases/v${VERSION}/version.txt
        cp src/mode.txt releases/v${VERSION}/mode.txt

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ steps.detect.outputs.version }}
        name: Kernel Module v${{ steps.detect.outputs.version }}
        files: |
          releases/v${{ steps.detect.outputs.version }}/module.ko
          releases/v${{ steps.detect.outputs.version }}/version.txt
          releases/v${{ steps.detect.outputs.version }}/mode.txt
        body: |
          ## Kernel Module Release v${{ steps.detect.outputs.version }}
          **Architecture:** ARM64 (Raspberry Pi)
          **Kernel:** 6.12.75+rpt-rpi-v8
          ### Security Tests
          ✅ cppcheck static analysis
          ✅ Hash integrity verification
          ✅ Signature tampering detection
          Ready for deployment.
